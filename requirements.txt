# --- Streamlit app ---
streamlit==1.49.0
yt-dlp
ffmpeg-python
pydub
python-dotenv
tqdm

# --- ML core (CPU only) ---
torch==2.2.2          # stable CPU wheel on Linux
transformers==4.41.2  # works with Flan-T5
sentence-transformers==3.0.1
faiss-cpu==1.7.4
huggingface-hub==0.24.5
pydantic>=1.10,<3

# --- ASR (Whisper via faster-whisper) ---
faster-whisper==1.0.1
ctranslate2==4.5.0

# --- Optional utilities you had (safe to keep) ---
httpx
