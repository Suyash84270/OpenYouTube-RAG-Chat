streamlit==1.49.0
yt-dlp
ffmpeg-python
pydub
python-dotenv
tqdm
httpx

# Avoid NumPy 2.x breaking wheels
numpy<2

# Transformers stack (CPU-friendly)
transformers==4.41.2
tokenizers==0.19.1
sentence-transformers==3.0.1
huggingface-hub==0.24.5
pydantic>=1.10,<3
accelerate==0.31.0

# Vector search (prebuilt wheel)
faiss-cpu==1.7.4

# Whisper (CPU)
faster-whisper==1.0.1
ctranslate2==4.5.0

# PyTorch CPU wheel (if this fails, try 2.1.2)
torch==2.2.2
